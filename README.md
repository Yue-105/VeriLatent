mkdir VeriLatent
cd VeriLatent

cat > README.md << 'EOF'
# VeriLatent

Official code for **Adaptive Inference-Time Scaling via Early-Step Latent Verification for Image Editing**.

Code coming soon.
EOF

git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Yue-105/VeriLatent.git
git push -u origin main
